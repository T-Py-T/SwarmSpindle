# Public delivery

Repository: https://github.com/T-Py-T/simpleswarmsystem (public, default branch main).

Published application commit: `b23594e0453a23eeab93904b4fe9bd4f1223a90c`. Job `job-mtrt69e3-41b82993` pushed this commit, cloned public main into a fresh directory, and passed locked installation, typecheck, build and all18 fresh-data web/worker checks. The public checkout matched the exact published commit. Snapshot:88 files, manifest SHA256 `5426432009c8ee93346e81d7bd0f86f594ace6357663369534aeb47e34ff014b`; installation/lifecycle check7.669seconds, graceful cleanup, no forced children, no provider calls.

The final source and initial history were inspected for credential/private runtime/media patterns. No workflow files exist. GitHub reported zero Actions runs after the initial push; no push-triggered workflow is configured. Original video/reference media, real Pi sessions, authentication, databases and private challenge exports were excluded. The README screenshot is a reviewed synthetic browser-test fixture and labeled accordingly. No upstream repository was changed.

Local application: http://127.0.0.1:5178/. Worker and web run separately on the same M4 Max. Readiness verified native Anthropic Opus4.8 High and Codex GPT-5.5 High, plus rootless Podman and its built offline image. Cursor is not integrated as a native Pi provider in this implementation.

Validation:137-case full integration (1,025 assertions), followed by typecheck and all46 affected runtime cases (377 assertions) after final diagnostic improvements. The standalone artifact verifier passed real browser SVG and22-second/DPR1/2/resize/60-second animated-canvas regressions, while rejecting synthetic fixtures as genuine acceptance.

Both requested30-peer swarms ran once with all30 actual exact-model responses, but both failed artifact acceptance under their original caps. [Acceptance](acceptance.md) preserves those outcomes. Public delivery and passing implementation tests do not make the missing challenge artifacts/signoffs pass. Additional paid diagnostic work or new challenge attempts requires a new budget decision; no historical liability will be cleared by source changes.
