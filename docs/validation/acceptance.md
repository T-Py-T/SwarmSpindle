# Genuine challenge acceptance

## Pelican: attempt ended, acceptance failed

Run `6cd93259-04d6-40b3-a348-de7aced63e92` used 30 actual native Pi sessions, `anthropic/claude-opus-4-8`, High effort, and one $50 USD-equivalent cap. All 30 distinct peers returned server-verified responses. The run ended `budget_exhausted`; four peers failed provider requests and 26 bailed. No peer reported done. Job `job-mtrsacdc-80739de6` exited 1.

| Accounting | USD equivalent |
| --- | ---: |
| Confirmed usage | $23.219985 |
| Uncertain liability retained for four requests | $21.600000 |
| Outstanding reservations | $0 |
| Available within original cap | $5.180015 |

The remaining allowance cannot admit the next $5.40 conservative request reservation. It is not evidence that $50 was billed. The four underlying provider failures are unknown: the original runtime retained liability but did not retain a safe HTTP status/error category. New safe HTTP/SSE diagnostic handling passed the 137-test integration before the canvas run; it cannot recover the unknown historical errors. No retry, replacement budget, or release of uncertain liability is authorized by this receipt.

Canonical `pelican.svg`, revision 1, is 7,902 bytes with SHA256 `e8164af85c3c86ed6cd69364eb7cb7d2012e67604d8d1dcac0710d72208d0801`. It was published 152.963 seconds after start, with $10.225310 already settled. The approximately $7.50 early-draft target was missed. The canonical file was never revised.

The external actual-Edge verifier passed SVG parsing, nonblank rendering, absence of scripts/raster/foreign objects/external references, no browser errors or blocked requests, all-peer session/model evidence, and unchanged canonical/ledger/evidence checks. Overall verification correctly failed because the runtime did not complete. The private export includes the exact workspace, receipt, 2,568 trace events, and external render. Original media and private sessions are excluded from publication.

Shared message 49 (author Beaker) and 50 (independent peer Pennib) contain quantitative geometry critiques. Pennib identifies an approximately 22-pixel wing-to-handlebar gap and a mostly hidden saddle. These defects were not corrected; no hash-bearing signoff exists in the final shared conversation. Two independent final-hash signoffs, resolved adversarial findings, and complete riding-contact acceptance therefore remain unmet. A legible illustration is not a passed challenge.

## Dashboard and worker evidence

With both HTTP dashboard tabs closed, the same actual worker continued: observed calls increased from 302 to 340 over 64.583 seconds. Reopening the dashboard recovered the live conversation and trace. Private before/after receipts retain worker identity and heartbeat. A local `file://` HTML tab is not a running dashboard; use `http://127.0.0.1:5178/`.

## Canvas: attempt ended, acceptance failed

Run `1433017e-354d-4fb4-b5d2-4c2121f9da48` started after the terminal pelican under job `job-mtrsvmsq-83198635`, using 30 native Pi `openai-codex/gpt-5.5` High peers and its separate $50 cap. All 30 distinct sessions returned server-verified GPT-5.5 responses (61 verified responses total). It ended `budget_exhausted`; two peers failed and 28 bailed, with no peer done.

| Accounting | USD equivalent |
| --- | ---: |
| Confirmed usage | $1.585418 |
| Uncertain liability retained for two requests | $32.520000 |
| Outstanding reservations | $0 |
| Available within original cap | $15.894582 |

The next conservative Codex reservation requires $16.26, so remaining allowance cannot admit another request. No extra budget, retry or liability release occurred. The two failures have no captured trustworthy HTTP/SSE category; their historical causes remain unknown. Source review confirms both requests reached the network wrapper; earlier local context conversion/compression failures do not fit the retained-attempt evidence. Unclassified connection failures, incomplete streams and SDK event-processing failures remain possible. The bounded diagnostic improvement passed typecheck and all46 affected runtime tests, including image-bearing intercepted requests; this cannot establish the historical cause. Their last tools read the original reference PNGs, which is a correlation rather than proof of an image-processing defect.

No canonical `shared/hero.html` or verification output was published. The exported workspace contains only the four original operator-seeded reference files. External artifact verification correctly exited1 for a missing file before launching a browser. The private export preserves the original run, budget, coordination and trace, with a separate missing-artifact report. Canvas motion/fidelity, 22-second captures, resize/DPR, 60-second stability and final-hash signoffs are all unmet; no synthetic or operator-authored artifact substitutes for this failed run.

The exact original reference article and two video frames are available privately. Continuous original-reference capture failed (headed capture timed out waiting for a canvas); do not claim a measured 22-second original recording. The verifier itself passed isolated SVG and 22-second/60-second animated-canvas regressions in `job-mtrsacie-751971db`, while correctly rejecting synthetic fixtures as genuine-model acceptance.
