# Dashboard comparison with the demonstration

Compared source-video frames 0137 (02:17), 0620 (10:20), and 0650 (10:50) against real Edge screenshots and the actual running pelican dashboard. Source frames and screenshots are retained privately outside the repository. Test screenshots contain synthetic records; the pelican observations contain actual Pi activity.

| Demonstrated element | Reconstruction and evidence |
| --- | --- |
| Cream surface, orange accents, fine separators, monospace operational text | Present in actual screenshots. Type sizing, spacing and exact colors differ; this is not a pixel-identical copy. |
| SWARMS / THREADS / AGENTS hierarchy | Same visible navigation and drill-down structure. Running and completed missions share one list. |
| Model, elapsed time, tokens, calls, spend and budget summary | Present, with explicit settled/reserved/uncertain values to distinguish temporary reservations from actual spend. |
| Thread rows, colored members, conversation overlay and original goal | Present. Conversations load incrementally; operator drafts retain focus, selection and scroll as messages arrive. |
| Raw trace, agent inspection and expanded event details | Present. Comparison exposed that an open trace required manual refresh; automatic append preserving expanded rows, focus and scroll passed in the10 actual Edge cases in job-mtrssovf-17c3dab4. |
| Activity histogram showing quiet periods and bursts | Present. The retained event window is labeled; exports retain the complete recorded trace. |
| Dense desktop controls | Core controls remain, with accessible launch/stop forms, worker/connection status, isolated artifact previews and phone layouts. |

The network logo and condensed display font were not copied. The original has a Preview/Raw conversation switch; this implementation presents safely escaped full conversation text and separately expandable raw trace data. Original internal routes and persistence format remain unverified.

## Actual browser independence

Pelican swarm `6cd93259-04d6-40b3-a348-de7aced63e92` appeared with 30 peers, exact Opus 4.8 High settings, conversations, tool activity and budget updates. All 30 peers returned server-verified model responses. Both HTTP dashboard tabs were closed, leaving only the unsupported local HTML tab. The same worker continued model responses and tool execution: calls advanced from 302 to 340 over the recorded 64.6-second interval, heartbeat advanced, and identity/PID remained unchanged. Reopening the dashboard recovered the current conversation and trace.

The final run had four provider failures with $21.60 of liability retained, and ended budget_exhausted. Failures remain distinct from bailed peers and are preserved in the final outcome; see acceptance.md.

The private simple-swarm-browser-final screenshots capture the tested live trace/composer behavior. Artifact acceptance and final challenge status remain separate requirements.
