# Support

> Tip-cite: `58f1f473 PR#<number>`; this is a provenance pointer only, not approval, and never a `READY` claim.

## Support boundary

This document provides operator guidance for the current `main` branch of the
local Bun and Pi swarm workspace. It is informational only: it is not a score,
approval, release certification, or readiness decision. Nothing here claims
`READY`.

## Getting support

When reporting a problem, include the affected commit, the command or workflow
that failed, the smallest reproducible example, and relevant local logs with
credentials and personal data removed. Do not share provider tokens, Pi
credentials, private configuration, or unredacted run exports.
